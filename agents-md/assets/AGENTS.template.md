<!--
AGENTS.md 模板 —— 由 agents-md skill 生成

填写指引：
1. 替换所有 {{占位符}} 为项目实际信息（搜索 "{{" 可定位所有待填项）
2. 删除不适用的章节（见每节顶部的「适用条件」注释）
3. 通用编码原则（第 3-6 节）建议原样保留，是跨项目验证的最佳实践
4. 项目特定层（红线、命令、Gotchas）必须实际核对，不可臆测
5. 篇幅控制：小项目 50-100 行，中型 150-250 行，大型 250-400 行

通用原则全文见 skill 的 references/principles.md；章节用途见 references/anatomy.md。
-->

# AGENTS.md

> 本仓库所有 AI agent（Claude Code、Codex 等）的行为指南。
> Claude Code 通过 `CLAUDE.md` 中的 `@AGENTS.md` 自动加载本文件。
>
> **{{项目名}}**：{{一句话定位}}。{{技术栈，如 "Next.js 14 + TypeScript + PostgreSQL"}}。

---

## 我们要解决什么

<!-- 适用条件：中型及以上项目必选；小项目可省 -->

LLM 写代码有几个典型陷阱：

- **臆测前提**：不假思索按错误假设执行，不主动澄清矛盾、不展示权衡
- **过度复杂**：堆抽象、堆配置、堆 fallback、不清理死代码
- **越界编辑**：动了与任务无关的代码、注释、格式
- **目标模糊**：不写"完成判据"就开工，靠手感判断对不对

下面 4 条原则正面回应这些陷阱。**红线规则**则是项目自身不能踩的硬约束。

---

## 总览：4 条编码原则

<!-- 通用层，建议原样保留。如项目有特殊原则可追加，但控制在 5 条以内 -->

| 原则 | 解决什么 |
|---|---|
| **编码前思考** | 错误假设、隐藏歧义、缺少权衡 |
| **简洁优先** | 过度抽象、未来主义、复杂兜底 |
| **精准修改** | 越界编辑、顺手重构、删不该删 |
| **目标驱动** | 没有验证标准，自我感觉良好 |

---

## 红线规则（违反必返工）

<!-- 项目特定层。按四类来源排查：一致性约束 / 工作流约束 / 安全约束 / 硬编码禁令。
     每条必须包含：触发条件 + 必做动作 + 后果。口号化红线无效。 -->

1. **{{一致性约束，如 "N 个模块一致性"}}**：{{触发条件，如 "改 [路径/trait] 后"}}，必须确认 {{N 个相关文件}} 仍能编译且行为一致。改完立刻跑 `{{验证命令}}`。
2. **{{工作流约束，如 "spec 驱动"}}**：代码改动前先 {{propose / 写测试 / 开 issue}}（{{例外情况}} 除外）。流程：{{步骤}}。
3. **不主动 push / tag**：未经用户明确指示，禁止 `git push`、`git tag`、commit 构建产物。
4. **{{硬编码禁令，如 "i18n 不硬编码"}}**：{{值类型}} 走 `{{配置位置}}`。代码内禁止 {{字面量写法}}。

---

## 原则详解

### 1. 编码前思考
> **不要臆测。不要藏起困惑。把权衡摊出来。**

- 假设说清楚——不确定就问，不要"猜一下试试"
- 歧义场景列出多种解读，请用户选，不要默默选一个
- 需求不清就停下来问，不要默默补需求
- 看到代码与意图矛盾，提出来，不要绕过

**检验标准：** 干完之后，用户是否会问"你为什么没问我 X？"

### 2. 简洁优先
> **能 50 行就别写 200 行。不为不可能的输入兜底。**

- 不加任务之外的功能、抽象、配置项
- 不为单次脚本设计抽象；重复 3 次再抽象
- 不可填的值用空值类型（`None` / `null`），不要兜底成空字符串或 `-1`
- 不为"将来可能扩展"留 unused 接口
- 不在内部代码里做防御性参数校验——只在系统边界（用户输入、外部 API）校验

**检验标准：** 资深工程师 review 时，会觉得这段实现"过度设计"吗？

### 3. 精准修改
> **只动用户要的那行。只清自己造的烂摊子。**

- 不顺手"改进"相邻代码、注释、空白、import 顺序
- 不重构能正常工作的代码——重构应单独立项
- 匹配现有风格，不要按个人偏好改命名/结构
- 看到无关死代码，**标注**而不是**删除**——让用户决定
- 改动产生的孤儿（本次引入的 unused import / var / fn）可以删；预先存在的死代码不要碰

**检验标准：** `git diff` 每一行都能追溯到本次任务吗？

### 4. 目标驱动
> **先写完成判据，再开工。验证到通过为止。**

把模糊指令转成可验证目标：

| 别这样 | 改成这样 |
|---|---|
| "加输入校验" | "为非法输入写测试，让它们通过" |
| "修 bug" | "写一个能复现 bug 的测试，让它通过" |
| "重构 X" | "重构前后跑一遍测试，断言行为不变" |
| "优化 UI" | "明确：哪个组件、哪种屏宽、对照截图/录屏" |

多步骤任务先列出验证点再做：

```
1. [步骤] → 验证：[可执行的检查]
2. [步骤] → 验证：[可执行的检查]
```

**检验标准：** 在动手前，能不能用一句话写出"完成的样子"？

---

## 反例对照（不要做 / 应该做）

<!-- 适用条件：中型及以上项目推荐。左列写项目真实踩过的坑，右列写正确做法 -->

| ❌ 不该做 | ✅ 应该做 |
|---|---|
| 改 bug 时顺手重构相邻函数 | 只改最小代码；重构开独立任务 |
| 为单次脚本加抽象 | 直接实现，重复 3 次再抽象 |
| 不可填值兜底用空字符串 / `-1` | 用空值类型，调用方显式处理 |
| 删除"看似无用"但不理解的代码 | 标注疑似死代码，让用户判断 |
| 为"以后可能要扩展"留 unused 接口 | 不留，需要时再加 |
| {{项目特定反例}} | {{项目特定正例}} |

---

## 命令速查

<!-- 项目特定层。必须实际核对命令可跑（查 package.json / Makefile / Cargo.toml）。
     务必注明项目没有的脚本及替代方案。 -->

```bash
{{dev 命令}}                          # 启动开发环境
{{build 命令}}                        # 生产构建
{{typecheck 命令}}                    # 类型检查（如无独立脚本，用 npx tsc --noEmit 等）
{{test 命令}}                         # 运行测试
{{lint 命令}}                         # 代码检查（如有）
```

**注意**：{{说明项目没有的脚本及替代方案，如 "无独立 typecheck 脚本，用 `npx tsc --noEmit`"}}。

---

## 架构 Gotchas

<!-- 项目特定层，信息密度最高的章节。只写"不看就会错"的反直觉知识，带具体路径/键名。
     删掉显而易见的条目（如"项目用 TypeScript"）。 -->

- **{{路径别名/约定}}**：{{反直觉事实，如 "@/ 指向 src/ui/ 而非 src/"}}。
- **{{状态管理}}**：{{位置与持久化键}}。
- **{{路由/注册}}**：{{新增需同步注册的位置}}。
- **{{数据层}}**：{{schema 改动需加 migration 等约束}}。
- **{{已知踩坑}}**：{{项目真实踩过的坑，指向详细文档}}。

---

## 提交前自检清单

<!-- 适用条件：中型及以上项目推荐。每个 checkbox 必须可执行（带命令），附条件标注 -->

- [ ] `{{typecheck}}` — 0 错误
- [ ] `{{lint}}` — 0 错误（如改了代码）
- [ ] `{{test}}` — 通过（如改了逻辑）
- [ ] {{一致性相关验证}}（如改了 {{模块}}）
- [ ] {{硬编码相关验证}}（如改了文案）
- [ ] `git diff` 每行都能追溯到当前任务

---

## 提交规范

<!-- 项目特定层。查 git log 看实际风格 -->

{{Conventional Commits（`feat` / `fix` / `refactor` / `chore`）}}。
正文写**改了什么、为什么改、影响范围**。bug fix 要写明根因。

---

## 文档要求

<!-- 适用条件：大型项目/团队必选；小项目可删此节。规范需与实际 docs/ 结构一致 -->

> {{如有文档规范总则，先指向它，如 "新建/编辑 docs/ 下文档前先读 docs/docs-guide.md"}}。

- **{{技术设计}}** 放 `{{docs/tech/}}`，文件名 `{{-design}}` 后缀。
- **{{PRD/需求}}** 放 `{{docs/prd/}}`。

{{两份文档必须互相引用。新增/删除文档后更新对应目录的 README.md。}}

---

## 关键参考

<!-- 项目特定层。相对路径链接 + 一句话说明。务必检查文件实际存在 -->

- `{{docs/xxx.md}}` — {{一句话说明作用}}
- `{{CHANGELOG / 架构决策记录 / spec 目录}}` — {{说明}}
